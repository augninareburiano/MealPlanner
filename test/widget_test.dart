// Placeholder test.
//
// Widget tests that boot the full app need Firebase to be initialized first,
// so for now this is just a trivial sanity check to keep `flutter test` green.
// Real tests (models, database mapping) can go here later.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('sanity check', () {
    expect(1 + 1, 2);
  });
}
